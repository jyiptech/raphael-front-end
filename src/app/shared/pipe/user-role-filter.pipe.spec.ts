import { UserRoleFilterPipe } from './user-role-filter.pipe';

describe('UserRoleFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UserRoleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
